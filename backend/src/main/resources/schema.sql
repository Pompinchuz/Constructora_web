-- ============================================
-- SCRIPT DE INICIALIZACIÓN DE BASE DE DATOS
-- ============================================
-- Este archivo se ejecuta automáticamente si configuras:
-- spring.sql.init.mode=always (solo para primera vez)

-- Nota: Hibernate creará las tablas automáticamente con ddl-auto=update
-- Este script es opcional y solo para referencia o inicialización manual

-- Si necesitas datos iniciales, crea un archivo data.sql
